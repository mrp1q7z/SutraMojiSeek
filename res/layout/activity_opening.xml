<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".OpeningActivity" >

    <ImageView
        android:id="@+id/imageView1"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="@dimen/activity_vertical_margin"
        android:src="@drawable/opening" />

    <Button
        android:id="@+id/start_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/imageView1"
        android:layout_centerHorizontal="true"
        android:drawableLeft="@drawable/btn_resume"
        android:onClick="onStartButtonClicked"
        android:text="@string/start" />

    <Button
        android:id="@+id/setting_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/start_button"
        android:layout_centerHorizontal="true"
        android:drawableLeft="@drawable/btn_setting"
        android:onClick="onSettingButtonClicked"
        android:text="@string/settings" />

    <Button
        android:id="@+id/usage_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/setting_button"
        android:layout_centerHorizontal="true"
        android:drawableLeft="@drawable/btn_usage"
        android:onClick="onUsageButtonClicked"
        android:text="@string/usage" />

    <Button
        android:id="@+id/ranking_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/usage_button"
        android:layout_centerHorizontal="true"
        android:drawableLeft="@drawable/btn_ranking"
        android:onClick="onRankingButtonClicked"
        android:text="@string/ranking" />

</RelativeLayout>